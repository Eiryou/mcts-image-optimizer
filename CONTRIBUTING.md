# Contributing

Thanks for your interest.

## Development Setup
```bash
python -m venv .venv
pip install -r requirements.txt
streamlit run app.py
```

## Guidelines
- Keep the demo responsive on small servers (Render free tier)
- Avoid heavy dependencies unless absolutely necessary
- Prefer clear logs and safe fallbacks
